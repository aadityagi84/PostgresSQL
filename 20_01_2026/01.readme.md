### Working with the Sql query and at all

- to create a table we will used the query

```
CREATE TABLE employee(
    emp_id SERIAL PRIMARY KEY AUTO INCREMENT,
    f_name VARCHAR(50) NOT NULL,
    l_name VARCHAR(50) NOT NULL,
    email VARCHAR(50) NOT NULL UNIQUE,
    dept VARCHAR(50) NOT NULL,
    salary DECIMAL(10,2) DEFAULT 3000.0,
    hire_at DATE NOT NULL CURRENT_DATE


 )
```

### the folder strucrture when u will be used the postgreSQL in nodejs and at all

- **Using PostgreSQL with stored procedures + Node.js + proper authentication is a very solid, production-grade setup.**

```
myapp-backend/
│
├── src/
│   ├── app.js
│   ├── server.js
│   │
│   ├── config/
│   │   ├── db.js
│   │   └── env.js
│   │
│   ├── routes/
│   │   ├── auth.routes.js
│   │   └── user.routes.js
│   │
│   ├── controllers/
│   │   ├── auth.controller.js
│   │   └── user.controller.js
│   │
│   ├── services/
│   │   ├── auth.service.js
│   │   └── user.service.js
│   │
│   ├── middlewares/
│   │   ├── auth.middleware.js
│   │   └── error.middleware.js
│   │
│   ├── utils/
│   │   ├── jwt.js
│   │   └── response.js
│   │
│   └── constants/
│       └── roles.js
│
├── database/
│   ├── schema/
│   │   ├── tables.sql
│   │   ├── roles.sql
│   │   └── permissions.sql
│   │
│   ├── functions/
│   │   ├── auth/
│   │   │   ├── register_user.sql
│   │   │   └── login_user.sql
│   │   │
│   │   └── user/
│   │       └── get_profile.sql
│   │
│   └── migrations/
│       └── 001_init.sql
│
├── .env
├── package.json
└── README.md

```
