### Crud in PostgreSql (Create, Read, Update, Delete)

- To check all of the databses which will be already created and how many databases in my databse to check this will be

- **1.**

```
 SELECT datname FROM  pg_database;
```

- **2.**

```
\l
```

## to Create Databse

```
CREATE DATABASE DATABSE_NAME;
```

## to chnage databse with commind line or SQLSHELL

```
\c DATABSENAME;
```

## to delete the database

```
DROP DATABASE DATABASE_NAME;
```

### Table

- **A table is a collection of related data held in table format with in databse**

## How to create a table in POSTGRESQL

- To create a tables first defined their roles and at all so

```
    CREATE TABLE <TABLE_NAME>(id INT, name VARCHAR(100), city VARCHAR(100));
```

## TO SEE THE TABLE IN DATABSE USE QUERY IN SQLSHELL

```
\d  TABLE_NAME
```

## to adding data inside the table so we will be used this query and thats was to be the

```
INSERT INTO <TABLE_NAME>(id,name, city) VALUES (101,'HELLO',HELLO');
```

- FOR EXAMPLES:

```
 INSERT INTO person(id,name,city) VALUES(101,'AADI','NOIDA');
```

```
 INSERT INTO person(id,name,city) VALUES(101,'AADI','NOIDA'),(102,'JAMES','NOIDA'),;
```
