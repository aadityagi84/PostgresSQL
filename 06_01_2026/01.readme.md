### Read Data from PostgreSQL

- use universalk query of sql

```
SELECT * FROM <-tablename->
```

```
SELECT <column_name> from <table_name>
```

### update data

- to update any table we will use **SQL** query

```

UPDATE <table_name> SET <column_name> = '<Value>' WHERE id="";
```

### Deleteing data from table and databse

- to delete the data from the tables

```
DELETE FROM <table_name> WHERE  name ="<value>";
```
